#include <stdio.h>
#include <stdlib.h>

int main ()
{
	int a = 32, int b = 8, int c;

	printf("Enter any value: ");
	scanf("%d", &c);

	int result = a*b*c;
	printf("Your final value is %d\n", result);

return (0);
}
